function sum(url) {
	const amount = var 
  const username = 'something';
  return username;
}

const array	= [
  'dbfbwf',
  'dbefvewiuydf',
  'dhydfywfgkijuw',
];

array.forEach((url) => {
  const username = sum(url);
  console.log(username);
});
